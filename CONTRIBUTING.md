# المساهمة في أدواتي (Contributing to Adawaty)

شكرًا لاهتمامك بالمساهمة. المشروع مصمم ليكون سهل التوسيع: كل أداة تعريف واحد، والصفحات تُولَّد تلقائيًا.

## المتطلبات

- Node.js >= 20
- فهم أساسي لـ ES Modules و HTML/CSS

## الإعداد المحلي

```bash
git clone https://github.com/abdelaziz-maysara-hm/adawaty.git
cd adawaty
npm install
```

## سير العمل المعتاد

1. اختر مهمة من [`docs/ROADMAP.md`](docs/ROADMAP.md) أو من قائمة التحسينات في `CHANGELOG.md` (قسم 0.5.38).
2. نفّذ التغييرات:
   - أدوات جديدة / تعديلات منطق → `src/product/definitions/` وملفات المعالجة ذات الصلة (`src/product/*-processing.js`).
   - لا تعدّل يدويًا مجلدات `tools/` أو `categories/` أو `all-tools/` (تُولَّد).
3. بعد أي تغيير في التعريفات أو سكربت التوليد:

```bash
npm run generate:product
```

4. تحقق قبل الـ commit:

```bash
npm run validate
```

هذا يشغّل فحص الصيغة + اختبارات التكامل (نفس ما يشغّله GitHub Actions).

5. حدّث:
   - `docs/ROADMAP.md` (ضع ✅ على الأداة المنفَّذة)
   - `CHANGELOG.md` (أضف سطر تحت الإصدار الحالي)
6. Commit برسالة واضحة ثم push.

## مبادئ مهمة

- **Client-side أولاً**: كل أداة جديدة يُفضَّل أن تعمل بالكامل داخل المتصفح بدون رفع ملفات.
- أدوات تحتاج سيرفر تبقى معزولة مع disclosure واضح للمستخدم.
- الدعم ثنائي اللغة (عربي + إنجليزي) مطلوب في العناوين والأوصاف وحقول الإدخال.
- ركّز على أدوات تحل مهمة حقيقية (معالجة ملفات، تحويلات عملية) بدل الحاسبات البسيطة المكررة.

## الإبلاغ عن مشاكل

افتح Issue على GitHub مع:
- رابط الأداة أو الصفحة
- خطوات إعادة الإنتاج
- المتصفح ونظام التشغيل
- لقطة شاشة إن أمكن

## الترخيص

بالمساهمة فإنك توافق على أن مساهمتك تُرخَّص تحت رخصة MIT (انظر `LICENSE`).
